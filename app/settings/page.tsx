import { auth } from "@/lib/auth"; import { redirect } from "next/navigation"; import Link from "next/link";
export default async function Settings(){const s=await auth();if(!s?.user)redirect("/login");return <main className="max-w-2xl mx-auto p-8"><Link href="/dashboard" className="underline">← Dashboard</Link><h1 className="text-3xl font-bold mt-8">Settings</h1><div className="mt-6 border rounded-2xl p-6 space-y-3"><h2 className="font-semibold">Profile</h2><p className="opacity-70">Name: {s.user.name||"Not set"}</p><p className="opacity-70">Email: {s.user.email}</p></div></main>}
