"use client";
import Link from "next/link"; import { useState } from "react"; import { signIn } from "next-auth/react"; import { useRouter } from "next/navigation";
export function LoginForm(){ const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false); const router=useRouter(); async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const r=await signIn("credentials",{email,password,redirect:false});setLoading(false);if(r?.error){setError("Invalid email or password.");return;}router.push("/dashboard");} return <form onSubmit={submit} className="w-full max-w-md border rounded-2xl p-8 space-y-5"><div><h1 className="text-2xl font-bold">Welcome back</h1><p className="opacity-60 text-sm mt-1">Sign in to your AI workspace.</p></div>{error&&<p className="text-sm text-red-600">{error}</p>}<input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} className="w-full border rounded-lg p-3 bg-transparent"/><input required type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full border rounded-lg p-3 bg-transparent"/><button disabled={loading} className="w-full rounded-lg bg-black text-white dark:bg-white dark:text-black p-3">{loading?"Signing in…":"Sign in"}</button><button type="button" onClick={()=>signIn("google",{callbackUrl:"/dashboard"})} className="w-full rounded-lg border p-3">Continue with Google</button><p className="text-sm text-center opacity-70">No account? <Link className="underline" href="/register">Create one</Link></p></form> }
